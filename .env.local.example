# ============================================================
# Ana Laura Noronha — variáveis de ambiente (EXEMPLO)
# Copie para .env.local e preencha. NÃO commitar .env.local.
# ============================================================

# --- Supabase (já existentes) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- Contato / Resend (já existente) ---
RESEND_API_KEY=
CONTACT_EMAIL_TO=analauramcb@hotmail.com
CONTACT_EMAIL_FROM=Site Ana Laura <orcamento@resend.dev>

# ============================================================
# Moodboard Generator (Fase 1)
# ============================================================

# Claude Vision — categorização de imagens (server-side apenas)
ANTHROPIC_API_KEY=

# Upstash Redis — rate limiting (falha graciosa se ausente)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Email do moodboard (Resend — reusa RESEND_API_KEY acima)
ANA_NOTIFICATION_EMAIL=contato@ananoronha.eng

# URL pública do site (usada nos emails)
NEXT_PUBLIC_SITE_URL=https://www.ananoronha.eng

# reCAPTCHA v3 (Fase 3 — placeholders reservados, ainda não usados)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=
