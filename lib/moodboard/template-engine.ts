/**
 * @deprecated Removido no v2 (moodboard painel-mental).
 * A lógica de slots fixos foi substituída por composição livre em
 * `./compose-collage.ts` (composeCollage).
 * Este arquivo existe apenas como marcador — pode ser deletado com segurança.
 */
export {};
